// LinkedIn OAuth public client ID only.
// The client secret must NEVER be stored in the Flutter app.
// Set it on the backend: firebase functions:secrets:set LINKEDIN_CLIENT_SECRET
//
// Copy to linkedin_secret.dart if you still have legacy imports, or remove
// those imports and use LinkedInOAuthConfig.clientId instead.
const String linkedinClientId = '86ltub3ua1c8o4';

@Deprecated('Client secret must not ship in the app. Use Cloud Functions.')
const String linkedinClientSecret = '';
